# Clothing Website API

Starter backend for a clothing website using Node.js, Express, and MongoDB.

## Setup

1. Copy `.env.example` to `.env` and update values.
2. Install dependencies:
   `npm install`
3. Run the server:
   `npm run dev`

## Endpoints

- `GET /health`
- `GET /api`
